{% extends "template.twig" %}

{% block content %}
    <div class="container margintop200">
        <div class="row">
            <h1>{{ post.title }}</h1>
        </div>
        <div class="row">
            <p>{{ post.content }}</p>
        </div>
    </div>

{% endblock %}