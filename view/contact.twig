{% extends "template.twig" %}

{% block content %}
<div class="container margintop200">
    <div class="row">
        <div class="col-md-12">
            <form action="index.php?action=addComment&amp;id=<?= $project['id'] ?>" method="POST">
                {% autoescape %}
                    {{ contactform.first_name|raw }}
                    {{ contactform.last_name|raw }}
                    {{ contactform.email|raw }}
                    {{ contactform.textarea|raw }}
                    {{ contactform.submit|raw }}
                {% endautoescape %}
            </form>
        </div>
    </div>
</div>
{% endblock %}





