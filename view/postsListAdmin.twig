{% extends "template.twig" %}
{% block content %}
<div class="container margintop200">
    {{ block("menuadmin", "adminMenu.twig") }}
    <div class="row">
        <div class="col-md-12">
            <a class="btn btn-dark" href="/admin/ajouter-un-projet">Ajouter un projet</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-12">
            <h2>Les projets</h2>
            {% for post in postlist  %}
                <div class="row">
                    <div class="col-md-12">
                        <a href="/admin/editer-projet/{{ post.id }}"><h3>{{ post.title }}</h3></a>
                        <p>{{ post.intro }}</p>
                        <a class="btn btn-primary" href="/admin/editer-projet/{{ post.id }}">Editer</a>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>
</div>
{% endblock %}
