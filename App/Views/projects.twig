{% extends "template.twig" %}

{% block content %}
    <div class="container margintop200">
        <div class="row">
            <div class="col-md-12">
                <h1>Mes derniers projets</h1>
                {% for project in projectlist  %}
                    <div class="mb-5">
                        <a href="projet/{{ project.slug }}-{{ project.id }}"><h3>{{ project.title|raw }}</h3></a>
                        <p>Modifié le : {{ project.modificationDate|date("d/m/Y")  }}</p>
                        <p>{{ project.intro|raw }}</p>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>

{% endblock %}